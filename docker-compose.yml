# 로컬 개발/테스트용
# 사용법: docker compose up --build
services:
  tessera:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ~/docker/tessera/data:/app/data
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
